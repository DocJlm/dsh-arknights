{
  "name": "dsh-arknights-collection",
  "version": "0.1.0",
  "description": "Community Arknights-themed skin collection for DeepSeek Harness Web",
  "private": true,
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": "^22.19 || >=24"
  },
  "packageManager": "pnpm@10.15.1",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck",
    "validate:repo": "node scripts/validate-repo.mjs",
    "check": "pnpm validate:repo && pnpm typecheck && pnpm test && pnpm build && pnpm validate:repo"
  }
}
